<input
        id="{{ id }}"
        type="{{ type }}"
        value="{{ value }}"
        placeholder="{{ placeholder }}"
        name="{{ name }}"
        class="input {{ class }}"
        autocomplete="{{ autocomplete }}"
>
