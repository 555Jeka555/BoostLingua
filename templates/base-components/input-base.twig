<input
        id="{{ id }}"
        type="{{ type }}"
        value="{{ value }}"
        placeholder="{{ placeholder }}"
        name="{{ name }}"
        class="{{ class }}"
        autocomplete="{{ autocomplete }}"
>
